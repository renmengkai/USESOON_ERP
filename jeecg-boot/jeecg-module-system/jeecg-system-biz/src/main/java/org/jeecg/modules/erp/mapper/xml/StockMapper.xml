<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.erp.mapper.StockMapper">

    <!-- 查询所有库存 -->
    <select id="selectAll" resultType="org.jeecg.modules.erp.entity.Stock">
        SELECT * FROM erp_stock
    </select>

    <!-- 根据 ID 查询库存 -->
    <select id="selectById" resultType="org.jeecg.modules.erp.entity.Stock">
        SELECT * FROM erp_stock WHERE id = #{id}
    </select>

    <!-- 插入库存 -->
    <insert id="insert">
        INSERT INTO erp_stock (
            id, productId, quantity, warehouse, location, lastUpdatedTime
        ) VALUES (
            #{id}, #{productId}, #{quantity}, #{warehouse}, #{location}, #{lastUpdatedTime}
        )
    </insert>

    <!-- 更新库存 -->
    <update id="updateById">
        UPDATE erp_stock
        SET
            productId = #{productId},
            quantity = #{quantity},
            warehouse = #{warehouse},
            location = #{location},
            lastUpdatedTime = #{lastUpdatedTime}
        WHERE id = #{id}
    </update>

    <!-- 删除库存 -->
    <delete id="deleteById">
        DELETE FROM erp_stock WHERE id = #{id}
    </delete>
</mapper>