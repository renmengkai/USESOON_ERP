<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.erp.mapper.CustomerMapper">

    <!-- 查询所有客户 -->
    <select id="selectAll" resultType="org.jeecg.modules.erp.entity.Customer">
        SELECT * FROM erp_customer
    </select>

    <!-- 根据 ID 查询客户 -->
    <select id="selectById" resultType="org.jeecg.modules.erp.entity.Customer">
        SELECT * FROM erp_customer WHERE id = #{id}
    </select>

    <!-- 插入客户 -->
    <insert id="insert">
        INSERT INTO erp_customer (
            id, name, contactPerson, phone, email, address, level,
            status, remark, tenantId, opter, opterId, optTime,
            crter, crterName, crteTime
        ) VALUES (
            #{id}, #{name}, #{contactPerson}, #{phone}, #{email}, #{address}, #{level},
            #{status}, #{remark}, #{tenantId}, #{opter}, #{opterId}, #{optTime},
            #{crter}, #{crterName}, #{crteTime}
        )
    </insert>

    <!-- 更新客户 -->
    <update id="updateById">
        UPDATE erp_customer
        SET
            name = #{name},
            contactPerson = #{contactPerson},
            phone = #{phone},
            email = #{email},
            address = #{address},
            level = #{level},
            status = #{status},
            remark = #{remark},
            tenantId = #{tenantId},
            opter = #{opter},
            opterId = #{opterId},
            optTime = #{optTime},
            crter = #{crter},
            crterName = #{crterName},
            crteTime = #{crteTime}
        WHERE id = #{id}
    </update>

    <!-- 删除客户 -->
    <delete id="deleteById">
        DELETE FROM erp_customer WHERE id = #{id}
    </delete>
</mapper>