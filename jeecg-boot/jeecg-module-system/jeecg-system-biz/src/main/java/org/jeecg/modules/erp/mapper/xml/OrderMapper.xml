<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.erp.mapper.OrderMapper">

    <!-- 查询所有订单 -->
    <select id="selectAll" resultType="org.jeecg.modules.erp.entity.Order">
        SELECT * FROM erp_order
    </select>

    <!-- 根据 ID 查询订单 -->
    <select id="selectById" resultType="org.jeecg.modules.erp.entity.Order">
        SELECT * FROM erp_order WHERE id = #{id}
    </select>

    <!-- 插入订单 -->
    <insert id="insert">
        INSERT INTO erp_order (
            id, orderNo, customerName, amount, status, orderDate, remark,
            productId, salePrice, number, nodeCode, estimatedDeliveryDate,
            actualDeliveryDate, deliveryRemark, estimatedPaybackDate,
            actualPaybackDate, paybackMethod, paybackRemark, tags,
            totalSalePrice, totalCostPrice, totalMargins, profitMargin,
            isValid, tenantId, opter, opterId, optTime, crter, crterName, crteTime
        ) VALUES (
            #{id}, #{orderNo}, #{customerName}, #{amount}, #{status}, #{orderDate}, #{remark},
            #{productId}, #{salePrice}, #{number}, #{nodeCode}, #{estimatedDeliveryDate},
            #{actualDeliveryDate}, #{deliveryRemark}, #{estimatedPaybackDate},
            #{actualPaybackDate}, #{paybackMethod}, #{paybackRemark}, #{tags},
            #{totalSalePrice}, #{totalCostPrice}, #{totalMargins}, #{profitMargin},
            #{isValid}, #{tenantId}, #{opter}, #{opterId}, #{optTime}, #{crter}, #{crterName}, #{crteTime}
        )
    </insert>

    <!-- 更新订单 -->
    <update id="updateById">
        UPDATE erp_order
        SET
            orderNo = #{orderNo},
            customerName = #{customerName},
            amount = #{amount},
            status = #{status},
            orderDate = #{orderDate},
            remark = #{remark},
            productId = #{productId},
            salePrice = #{salePrice},
            number = #{number},
            nodeCode = #{nodeCode},
            estimatedDeliveryDate = #{estimatedDeliveryDate},
            actualDeliveryDate = #{actualDeliveryDate},
            deliveryRemark = #{deliveryRemark},
            estimatedPaybackDate = #{estimatedPaybackDate},
            actualPaybackDate = #{actualPaybackDate},
            paybackMethod = #{paybackMethod},
            paybackRemark = #{paybackRemark},
            tags = #{tags},
            totalSalePrice = #{totalSalePrice},
            totalCostPrice = #{totalCostPrice},
            totalMargins = #{totalMargins},
            profitMargin = #{profitMargin},
            isValid = #{isValid},
            tenantId = #{tenantId},
            opter = #{opter},
            opterId = #{opterId},
            optTime = #{optTime},
            crter = #{crter},
            crterName = #{crterName},
            crteTime = #{crteTime}
        WHERE id = #{id}
    </update>

    <!-- 删除订单 -->
    <delete id="deleteById">
        DELETE FROM erp_order WHERE id = #{id}
    </delete>
</mapper>